import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.nakshatranow',
  appName: 'NakshatraNow',
  webDir: 'out' // Use the static export output directory
};

export default config;
